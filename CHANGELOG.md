# Changelog

All notable changes to this project will be documented in this file.

## [0.3.0] - 2026-04-30

### 🚀 Features

- Add LIKE operator support to query builder
- Add HAVING clause support with aggregate functions
- Add gitsign signing, automated release workflow, and contribution guidelines
- Use sqlx attributes for `rename` and `skip` directly
- *(macros)* Add `#[automatically_derived]` to all generated `impl` blocks for improved tooling support (#13)
- *(macros)* [**breaking**] Migrate is_set attribute to darling Callable (#14)
- *(macros)* Support #[sqlx(json)] attribute for JSON column binding (#15)
- *(macros)* Support #[sqlx(flatten)] with #[lorm(flattened(...))] (#16)
- Composite primary key support (manual pk_type) (#17)
- *(macros)* Conditional upsert for manual pk save() (#18)

### 🐛 Bug Fixes

- Run verify-signatures workflow on pushes to main
- *(is_set)* Call `::default()` on field type instead of `Default`
- Make postgres and mysql backends functional
- *(macros)* Correct error message for `#[lorm(new = "...")]` attribute validation (#13)

### 🚜 Refactor

- Update github actions and toolchain to match.
- Improve test structure and reduce duplication
- Removes idea folder as it is unrelated.
- Replace Inflector with heck for snake_case conversion
- *(ci)* Rename verify-signatures workflow to verify-gitsign-signatures
- Add darling dependency
- Use darling for attribute parsing
- Change argument types for query functions

### 📚 Documentation

- Replace gitsign-specific signing docs with GitHub signing references
- Update README for multi-backend support and MySQL limitations

### 🧪 Testing

- Add field with rename attribute to test
- Allow unused updated_at fields

### ⚙️ Miscellaneous Tasks

- Remove verify-gitsign-signatures workflow and references
- Include Cargo.lock lockfile
- Test all feature gates in CI with postgres and mysql service containers
- Pre-0.3.0 cleanup and fixes from PR #2 review

<!-- generated by git-cliff -->
